name: Deploy to Cloudflare development
on:
  push:
    branches:
    - develop
jobs:
  deploy:
    name: Build and deploy site to Cloudflare
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@master
    - name: Build
      run: swift build
    - name: Publish
      uses: uses: cloudflare/wrangler-action@1.1.0
      with:
        apiToken: ${{ secrets.CF_API_TOKEN }}
        workingDirectory: Cloudflare